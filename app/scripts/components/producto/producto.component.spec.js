describe('Component: .', () => {
  let $controller;

  beforeEach(() => {
    module('');

    inject((_$componentController_) => {
      $controller = _$componentController_('');
    });
  });

  describe(``, () => {

  });
});
